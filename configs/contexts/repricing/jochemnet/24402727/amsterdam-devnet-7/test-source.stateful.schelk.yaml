runner:
  benchmark:
    tests:
      metadata:
        labels:
          name: jochemnet-24402727-amsterdam-stateful
          chain: jochemnet
          block: "24402727"
          test-type: stateful
          context: repricing
          fork: amsterdam
          data-disk-type: schelk

      source:
        archive:
          file: https://github.com/NethermindEth/gas-benchmarks/releases/download/amsterdam-repricings-v5.2.0/generated-tests-stateful-jochemnet.tar.gz

          # NOTE: When using schelk, we need to remove the pre-runs. They have already been included in the schelk snapshot.
          #pre_run_steps:
          #  - "repricings_stateful/jochemnet/gas-bump.txt"
          #  - "repricings_stateful/jochemnet/funding.txt"
          steps:
            setup:
              - "repricings_stateful/jochemnet/setup/*.txt"
            test:
              - "repricings_stateful/jochemnet/testing/*.txt"
      opcode_source:
        file: https://github.com/NethermindEth/gas-benchmarks/releases/download/amsterdam-repricings-v5.2.0/opcodes_tracing-stateful-jochemnet.json

  container_runtime: podman
  client:
    config:
      rollback_strategy: container-recreate
