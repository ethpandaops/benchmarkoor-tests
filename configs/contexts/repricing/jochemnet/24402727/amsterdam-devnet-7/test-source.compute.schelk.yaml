runner:
  benchmark:
    tests:
      metadata:
        labels:
          name: jochemnet-24402727-amsterdam-compute
          chain: jochemnet
          block: "24402727"
          test-type: compute
          context: repricing
          fork: amsterdam
          data-disk-type: schelk

      source:
        archive:
          file: https://github.com/NethermindEth/gas-benchmarks/releases/download/amsterdam-repricings-v5.2.0/generated-tests-compute-jochemnet.tar.gz
          # NOTE: When using schelk, we need to remove the pre-runs. They have already been included in the schelk snapshot.
          #pre_run_steps:
          #pre_run_steps:
          #  - "repricings_compute/jochemnet/gas-bump.txt"
          #  - "repricings_compute/jochemnet/funding.txt"
          steps:
            setup:
              - "repricings_compute/jochemnet/setup/*.txt"
            test:
              - "repricings_compute/jochemnet/testing/*.txt"
      opcode_source:
        file: https://github.com/NethermindEth/gas-benchmarks/releases/download/amsterdam-repricings-v5.2.0/opcodes_tracing-compute-jochemnet.json


  container_runtime: podman
  client:
    config:
      rollback_strategy: none
